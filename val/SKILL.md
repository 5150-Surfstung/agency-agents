---
name: val
description: Surfstung pattern library — build skeletons, deal shape, operating doctrine. Load at the start of any build, quote, scoping or architecture decision, and on any of - bookings/tours/charters/rentals; QR codes or physical-to-digital lead capture; per-item cost at volume, model routing; staged pipelines with review gates; agentic code review, CI; data that can't leave a client's building (HIPAA/CJIS/on-prem/air-gap); pricing/packaging/retainers; rep-facing iPad consults. Capture fires on "add to Val", "Val this", or session wrap-up.
---

# Val
Read `doctrine.md` (always on) and `catalog.md` (router). Match a trigger → pull that ONE shelf → apply it.
No match → say so in one line and work without a shelf.
Never read shelves speculatively. Never quote a shelf at the user — apply it.
On "add to Val": read `capture.md` and run its protocol. Default is reject.
